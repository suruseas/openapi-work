type: array
# prefixItems:
#   - type: integer
#     description: ID
#   - type: string
#     description: Book name
#   - type: string
#     description: Book author
# minItems: 2
# maxItems: 2
# additionalItems: false
items:
  oneOf:
    - type: integer
      example: 1
    - type: string
      example: 1984
    - type: string
      example: George Orwell.
minItems: 3
maxItems: 3
description: >-
  The array positions have meaning. The first item is the ID, the second is the book name, and the third is the book author.
